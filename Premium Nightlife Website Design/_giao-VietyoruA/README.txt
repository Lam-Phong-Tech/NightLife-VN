GIAO DIỆN KHÁCH — PHƯƠNG ÁN A (Premium Elegant · than đen + Gold)
=================================================================

Nội dung gói này = phần đã chuyển sang Phương án A cho cụm KHÁCH:
  app/desktop/   (20 trang)
  app/mobile/    (23 trang)
  app/shared.js  (helper: chip/pill/tab/badge/heart → gold)
  SVG/           (14 icon line màu gold)

CÁCH ÁP DỤNG
------------
Chép đè 4 mục trên vào D:\Wirefame_Nightlight (giữ nguyên cấu trúc thư mục).
Mở lại app/desktop/trang-chu.html hoặc app/mobile/trang-chu.html để xem.

ĐÃ THAY ĐỔI
-----------
• Nền tối #0c0c0f, chữ kem #f3f0ea; điểm nhấn Gold (#d4b26a / gradient).
• CTA = gold-gradient, chữ nâu #241a0a. Thẻ VIP/hạng nền gold.
• Mỗi section: tiêu đề + nhãn EN nhỏ + divider gold tan dần.
• Pill "ĐANG DIỄN RA" chấm hồng neon; icon đổi sang line gold.
• Bỏ chế độ Sáng/Tối — chỉ còn Tối (đã gỡ tham chiếu theme.js trong các trang khách).

LƯU Ý
-----
• app/shared.js và SVG/ DÙNG CHUNG với cụm Partner/Admin. Sau khi chép đè,
  các nút/chip/icon ở Partner/Admin cũng sẽ chuyển tông gold. Nếu muốn giữ
  Partner/Admin như cũ, chỉ chép app/desktop + app/mobile và tự quản lý shared.js.
• theme.js không còn dùng cho cụm khách (có thể giữ lại, vô hại).
• support.js giữ nguyên bản hiện có của bạn.
