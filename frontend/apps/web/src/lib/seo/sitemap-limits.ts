/**
 * sitemap-limits.ts
 * Centralised constants for discovery API limits used in sitemap generation.
 * Extracted so they can be tested and changed in one place.
 */

export const SITEMAP_STORE_LIMIT = 500;
export const SITEMAP_CAST_LIMIT = 500;
